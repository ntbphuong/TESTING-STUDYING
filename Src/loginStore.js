function login(emailInput, passwordInput){
    //1.Kiem tra email
    if(!emailInput) {
        return "Vui long nhap email"
    }
    //1.Kiem tra email
    if(!emailInput) {
        return "Vui long nhap email"
    }
    //1.Kiem tra email
    if(!emailInput) {
        return "Vui long nhap email"
    }
}